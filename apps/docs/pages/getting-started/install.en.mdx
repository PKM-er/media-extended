---
title: Installation
---

# Plugin Installation

import { getSSGProps } from "@/components/install/obsidian/data";
import ObsidianInstall from "@/components/install/obsidian/main.en";

export const getStaticProps = () =>
  getSSGProps().then((ssg) => ({ props: { ssg }, revalidate: 60 }));

<ObsidianInstall />
